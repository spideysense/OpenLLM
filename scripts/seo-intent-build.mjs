#!/usr/bin/env node
import { mkdirSync, readFileSync, writeFileSync } from 'node:fs';
import { join, resolve } from 'node:path';

const SITE_DIR = resolve('site');
const ORIGIN = 'https://www.runonaspen.com';
const UPDATED = '2026-09-07';

export const HUBS = [
  {
    slug: 'local-ai',
    title: 'Local AI: Run AI Privately on Your Own Computer | Aspen',
    description: 'What local AI is, how it works, what hardware you need, and when it beats cloud AI. Aspen runs private AI locally on Mac, Windows and iPhone.',
    h1: 'Local AI: run AI on your own computer',
    intro: 'Local AI means the model that answers you runs on hardware you control instead of a remote cloud service. Once a model is downloaded, core chat and coding can run on-device and offline. Aspen packages the model, inference engine and interface into a normal app so you do not need to manage a terminal or server.',
    sections: [
      ['What local AI actually changes', `<p>With cloud AI, your prompt travels to a provider's infrastructure, the model runs there, and a response comes back over the internet. With local AI, inference happens on your Mac or PC. That changes the privacy boundary, cost model and availability of the system.</p><ul><li><strong>Privacy:</strong> core prompts and files can stay on your own device.</li><li><strong>Offline use:</strong> after the model is downloaded, chat and coding do not require a connection.</li><li><strong>No per-token meter:</strong> you use your own compute rather than paying for every request.</li><li><strong>Control:</strong> you can choose among open models and change them as your needs change.</li></ul>`],
      ['What local AI is good at', `<p>Modern open models are useful for writing, summarizing, brainstorming, coding help, document work and many everyday questions. They are also useful when the input is sensitive or when internet access is unreliable.</p><p>The tradeoff is straightforward: the largest cloud models still have an advantage on the hardest reasoning tasks, while a local model is limited by the memory and compute available on your machine. Many people use both: local AI for routine and private work, and a frontier cloud model when they explicitly need maximum capability.</p>`],
      ['What hardware do you need?', `<p>A modern laptop or desktop is enough to start. As a rough guide, 8GB of memory supports small models, 16GB is comfortable for many 7–8B models, 32GB opens up 13–14B-class models, and higher-memory machines can run larger options. Quantization reduces memory needs. Apple Silicon is particularly convenient because CPU and GPU share unified memory.</p><p>Aspen checks your hardware and recommends models that fit rather than asking you to reason about parameter counts and quantization files yourself.</p>`],
      ['How to start with Aspen', `<ol><li>Download Aspen for Mac or Windows, or install Aspen Local AI on iPhone.</li><li>Open the app and choose a recommended model.</li><li>Download the model once.</li><li>Chat, code or work with your local model. Core use continues to work offline.</li></ol><p>No account or subscription is required for the app. Optional online tools such as live web search still need a network connection.</p>`],
      ['Local AI vs local LLM', `<p><em>Local AI</em> is the broader category: an AI capability running on your own hardware. A <a href="/local-llm/">local LLM</a> is specifically a large language model running locally. Aspen uses local LLMs for chat and coding, and exposes them to other software through an <a href="/openai-compatible-local-api/">OpenAI-compatible local API</a>.</p>`],
    ],
    faq: [
      ['Does local AI work without internet?', 'Yes for core inference after the model is downloaded. Internet is still needed to download new models or use explicitly online tools such as live web search.'],
      ['Is local AI free?', 'Open-weight models can be downloaded and run without per-token fees. Aspen’s Mac, Windows and iPhone apps are free; your machine supplies the compute and electricity.'],
      ['Is local AI as good as ChatGPT?', 'For many everyday writing, summarization and coding tasks, modern local models are strong. The largest cloud models still lead on some difficult reasoning tasks, so the right choice depends on the job.'],
    ],
  },
  {
    slug: 'local-llm',
    title: 'Local LLM: Run Large Language Models on Mac & Windows | Aspen',
    description: 'A practical guide to local LLMs: model sizes, RAM, quantization, CPU vs GPU, privacy, offline use and the easiest way to run one with Aspen.',
    h1: 'Local LLM: run a large language model on your own machine',
    intro: 'A local LLM is a large language model whose weights are stored on your computer and whose responses are generated by your own CPU or GPU. Instead of sending every prompt to a cloud provider, you download a model once and run inference locally.',
    sections: [
      ['How a local LLM works', `<p>A local LLM setup has three pieces: the model weights, an inference engine that turns those weights into generated text, and an interface or API. Aspen bundles those pieces so choosing and running a model feels like using a regular application.</p><p>The model generates text token by token. The speed and maximum model size depend mainly on available memory and compute.</p>`],
      ['Model size and memory', `<p>Model names often include a parameter count such as 3B, 8B, 14B or 32B. Larger models generally need more memory. A practical rule of thumb is: 8GB for small models, 16GB for many 7–8B models, 32GB for many 13–14B models, and 64GB or more for larger models. Exact requirements vary with architecture and quantization.</p><p>Quantization stores model values at lower precision, shrinking memory use and often improving speed with a modest quality tradeoff. It is a major reason capable models can run on consumer hardware.</p>`],
      ['CPU, GPU and Apple Silicon', `<p>You can run an LLM without a dedicated GPU, although generation is usually slower. A recent discrete GPU improves throughput and makes larger models practical. Apple Silicon is a strong local-LLM platform because its unified memory can be used efficiently by the GPU rather than splitting system RAM and VRAM into separate pools.</p>`],
      ['Which local model should you choose?', `<p>There is no universal best model. The best choice is the strongest model that fits your hardware comfortably and performs well on your task. Families such as Llama, Qwen, DeepSeek, Mistral and Gemma offer different size and capability tradeoffs.</p><p>Aspen detects your hardware and recommends a sensible default. You can switch models later instead of locking your workflow to one provider.</p>`],
      ['When local LLMs make sense', `<p>Local LLMs are especially attractive for private documents, offline work, repeated high-volume use, development against an AI API, and workflows where you want predictable ownership of the model and data path. For the hardest frontier reasoning, a cloud model may still be the better tool.</p><p>For a broader introduction, read the <a href="/local-ai/">local AI guide</a>. If privacy is the primary reason you are going local, see <a href="/private-ai/">private AI</a>.</p>`],
    ],
    faq: [
      ['How much RAM do I need for a local LLM?', 'As a rough guide: 8GB for small models, 16GB for many 7–8B models, 32GB for many 13–14B models, and 64GB or more for larger options. Quantization changes the exact requirement.'],
      ['Can I run a local LLM on a MacBook?', 'Yes. Apple Silicon Macs are well suited to local inference because they use unified memory. The model size you can run comfortably depends on the Mac’s memory.'],
      ['Do I need a GPU?', 'No. CPU-only inference works for many models, but a capable GPU or Apple Silicon usually improves speed.'],
    ],
  },
  {
    slug: 'private-ai',
    title: 'Private AI: Keep Chats and Files on Your Own Device | Aspen',
    description: 'Private AI explained: what stays local, what can still use the network, and how on-device LLMs reduce exposure of sensitive chats and files.',
    h1: 'Private AI: keep the model and your work on your device',
    intro: 'Private AI is an architecture choice, not a marketing checkbox. The strongest privacy boundary is to run the model on hardware you control so core prompts, files and generated responses do not need to be sent to a third-party inference server.',
    sections: [
      ['Local inference changes the data path', `<p>In a typical cloud chatbot, your text or files are transmitted to remote infrastructure for inference. With Aspen, the selected model runs on your own machine. Core local chat and coding therefore do not require Aspen to receive your conversation.</p><p>That does not mean every optional feature is magically offline. If you deliberately invoke a networked tool such as live web search, that tool must make a network request. The useful distinction is that the model itself can run locally and the user chooses when an online capability is invoked.</p>`],
      ['What stays local with Aspen', `<ul><li>The downloaded model weights live on your machine.</li><li>Core inference runs on your hardware.</li><li>Local conversations and files do not need a cloud inference service.</li><li>Core chat and coding can continue with no internet connection after the model is present.</li></ul><p>This makes local AI useful for drafts, internal documents, source code, notes and other material you would rather not send to a hosted model by default.</p>`],
      ['Private does not mean invulnerable', `<p>Your computer still needs normal security hygiene. Malware, an unlocked device, insecure backups, overly broad file permissions or a network tool you explicitly call can create exposure. Local inference reduces one important class of data sharing; it does not replace operating-system security, encryption, access controls or sensible credential handling.</p>`],
      ['Cloud AI vs private local AI', `<p>Cloud AI is convenient because the provider supplies enormous compute and can update models instantly. Local AI trades some of that centralized scale for control, offline availability and a smaller default data path. The right answer can be hybrid: keep ordinary or sensitive work local and choose cloud inference only when the task warrants it.</p>`],
      ['Start with a private local model', `<p>Install Aspen, choose a model that fits your computer, download it, and use local chat or coding without an account or subscription. Developers can also keep application inference local by pointing compatible tools at Aspen’s <a href="/openai-compatible-local-api/">OpenAI-compatible API</a>.</p>`],
    ],
    faq: [
      ['Does Aspen send my prompts to a cloud model?', 'Core local inference runs on the model on your own hardware. Optional online tools can make network requests when you choose to use them.'],
      ['Can local AI work offline?', 'Yes. Once a model is downloaded, Aspen’s core local chat and coding can run without internet access.'],
      ['Is local AI automatically secure?', 'Local inference reduces data sent to third-party inference servers, but you still need normal device security, encryption, access controls and careful handling of credentials.'],
    ],
  },
  {
    slug: 'openai-compatible-local-api',
    title: 'OpenAI-Compatible Local API: Use Apps with a Local LLM | Aspen',
    description: 'Run an OpenAI-compatible API locally with Aspen at localhost:4000/v1. Point OpenAI-style clients, LangChain, Cursor and other tools at your own LLM.',
    h1: 'OpenAI-compatible local API',
    intro: 'Aspen exposes a local gateway that speaks the OpenAI API format. That lets software written for an OpenAI-style client use a model running on your own computer by changing the base URL and API key rather than rewriting the application.',
    sections: [
      ['The local endpoint', `<p>On the machine running Aspen, the OpenAI-compatible base URL is:</p><pre><code>http://localhost:4000/v1</code></pre><p>Use an Aspen API key rather than a cloud-provider key. Because the gateway is local, requests can be handled by the model running on your own hardware.</p>`],
      ['OpenAI Python SDK example', `<pre><code>from openai import OpenAI

client = OpenAI(
    base_url="http://localhost:4000/v1",
    api_key="YOUR-ASPEN-KEY",
)

response = client.chat.completions.create(
    model="your-local-model",
    messages=[{"role": "user", "content": "Explain this code."}],
)

print(response.choices[0].message.content)</code></pre><p>The important integration change is the <code>base_url</code> and API key. Model names depend on what you have installed and selected in Aspen.</p>`],
      ['What can connect?', `<p>Any tool that can target an OpenAI-compatible endpoint can potentially use the gateway. Aspen’s documentation specifically covers OpenAI-style clients and integrations such as LangChain, Cursor, Continue.dev, n8n and Zapier. Compatibility can vary by the exact features a third-party tool expects, so test the endpoints your application actually uses.</p>`],
      ['Why use a local API?', `<ul><li>Keep inference on hardware you control.</li><li>Develop without a per-token cloud bill for local requests.</li><li>Reuse OpenAI-style client libraries and tooling.</li><li>Run application workflows even when internet access is unavailable, as long as the workflow itself does not depend on online tools.</li></ul>`],
      ['Remote access and keys', `<p>Aspen can also expose a private HTTPS URL so your own devices or applications can reach the AI running on your computer remotely. Aspen supports different key types for different trust levels. Treat an Owner key as highly privileged and use the least-privileged key appropriate for a client.</p><p>See the <a href="/docs">Aspen documentation</a> for current API and key details, or start with the <a href="/local-llm/">local LLM guide</a>.</p>`],
    ],
    faq: [
      ['What is Aspen’s OpenAI-compatible base URL?', 'For local access on the machine running Aspen, use http://localhost:4000/v1.'],
      ['Do I use my OpenAI API key?', 'No. Point the client at Aspen’s base URL and use an Aspen API key.'],
      ['Can existing OpenAI-style apps use Aspen?', 'Many can. If the application lets you configure an OpenAI-compatible base URL and API key, it can often point at Aspen without a major rewrite. Test the specific API features your app relies on.'],
    ],
  },
];

const escapeJson = (value) => JSON.stringify(value).replaceAll('<', '\\u003c');

function pageHtml(page) {
  const canonical = `${ORIGIN}/${page.slug}/`;
  const related = HUBS.filter((item) => item.slug !== page.slug)
    .map((item) => `<li><a href="/${item.slug}/">${item.h1}</a></li>`).join('');
  const faqHtml = page.faq.map(([q, a]) => `<details><summary>${q}</summary><p>${a}</p></details>`).join('');
  const schemas = [
    {
      '@context': 'https://schema.org', '@type': 'WebPage', name: page.h1,
      url: canonical, description: page.description, dateModified: UPDATED,
      isPartOf: { '@type': 'WebSite', name: 'Aspen', url: `${ORIGIN}/` },
    },
    {
      '@context': 'https://schema.org', '@type': 'FAQPage',
      mainEntity: page.faq.map(([q, a]) => ({ '@type': 'Question', name: q, acceptedAnswer: { '@type': 'Answer', text: a } })),
    },
  ];
  return `<!DOCTYPE html>
<html lang="en"><head>
<meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1.0">
<title>${page.title}</title>
<meta name="description" content="${page.description}">
<link rel="canonical" href="${canonical}">
<meta property="og:type" content="article"><meta property="og:site_name" content="Aspen">
<meta property="og:title" content="${page.title}"><meta property="og:description" content="${page.description}"><meta property="og:url" content="${canonical}">
<meta property="og:image" content="${ORIGIN}/og-image.png"><meta name="twitter:card" content="summary_large_image"><meta name="twitter:title" content="${page.title}"><meta name="twitter:description" content="${page.description}">
<script type="application/ld+json">${escapeJson(schemas)}</script>
<style>*{box-sizing:border-box}body{margin:0;font-family:Georgia,serif;background:#faf8f5;color:#272727;line-height:1.75}.nav{padding:1rem 1.5rem;border-bottom:1px solid #e8e3dc;display:flex;gap:1.2rem;align-items:center}.nav a,a{color:#9b7209}.nav a{text-decoration:none;font-weight:700}.wrap{max-width:760px;margin:0 auto;padding:3.5rem 1.5rem 5rem}h1{font-size:clamp(2.25rem,6vw,4rem);line-height:1.08;margin:0 0 1.2rem;color:#171717}h2{font-size:1.55rem;line-height:1.25;margin:2.5rem 0 .75rem;color:#171717}.lede{font-size:1.18rem;color:#555;margin-bottom:2.5rem}p{margin:0 0 1.1rem}li{margin:.45rem 0}pre{overflow:auto;background:#171717;color:#eee;padding:1rem;border-radius:10px}code{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:.9em}.related,.cta{margin-top:3rem;padding:1.5rem;border:1px solid #e3dac9;border-radius:14px;background:#fff}.cta{text-align:center}.cta a{display:inline-block;margin-top:.5rem;background:#9b7209;color:white;text-decoration:none;padding:.7rem 1rem;border-radius:8px;font-weight:700}details{border-top:1px solid #e8e3dc;padding:.9rem 0}summary{font-weight:700;cursor:pointer}footer{text-align:center;padding:2rem;color:#777;font-size:.85rem}</style><link rel="stylesheet" href="/leaf.css">
</head><body>
<nav class="nav"><a href="/"><img src="/leaf.svg" class="leaf" alt=""> Aspen</a><a href="/docs">Docs</a><a href="/blog/">Blog</a></nav>
<main class="wrap"><p><a href="/">Aspen</a> / Guide</p><h1>${page.h1}</h1><p class="lede">${page.intro}</p>
${page.sections.map(([heading, html]) => `<section><h2>${heading}</h2>${html}</section>`).join('\n')}
<section><h2>Frequently asked questions</h2>${faqHtml}</section>
<section class="related"><h2>Explore the local AI guides</h2><ul>${related}</ul><p>For tutorials and comparisons, visit the <a href="/blog/">Aspen blog</a>.</p></section>
<section class="cta"><h2>Run AI on your own hardware</h2><p>Aspen is free on Mac, Windows and iPhone. No subscription or account required.</p><a href="/#download">Get Aspen free →</a></section>
</main><footer>© 2026 Aspen · <a href="/">Home</a> · <a href="/docs">Docs</a> · <a href="/blog/">Blog</a></footer>
</body></html>`;
}

export function writeIntentHubs(siteDir = SITE_DIR) {
  for (const page of HUBS) {
    const dir = join(siteDir, page.slug);
    mkdirSync(dir, { recursive: true });
    writeFileSync(join(dir, 'index.html'), pageHtml(page));
  }
}

export function addHubsToSitemap(siteDir = SITE_DIR) {
  const path = join(siteDir, 'sitemap.xml');
  let xml = readFileSync(path, 'utf8');
  const blocks = HUBS.filter((page) => !xml.includes(`${ORIGIN}/${page.slug}/`)).map((page) => `  <url>\n    <loc>${ORIGIN}/${page.slug}/</loc>\n    <lastmod>${UPDATED}</lastmod>\n    <changefreq>monthly</changefreq>\n    <priority>0.8</priority>\n  </url>`).join('\n');
  if (blocks) xml = xml.replace('</urlset>', `${blocks}\n</urlset>`);
  writeFileSync(path, xml);
}

export function linkHubsFromBlog(siteDir = SITE_DIR) {
  const path = join(siteDir, 'blog', 'index.html');
  let html = readFileSync(path, 'utf8');
  if (html.includes('data-aspen-intent-hubs')) return;
  const cards = HUBS.map((page) => `<a href="/${page.slug}/" class="post-card"><div class="post-date">Evergreen guide</div><h2 class="post-title">${page.h1}</h2><p class="post-desc">${page.description}</p></a>`).join('\n');
  const block = `<div data-aspen-intent-hubs><h2 style="margin:0 0 1rem">Start here: local AI guides</h2>${cards}</div>`;
  html = html.replace('<div class="posts">', `<div class="posts">\n${block}`);
  writeFileSync(path, html);
}

export function appendHubsToLlms(siteDir = SITE_DIR) {
  const path = join(siteDir, 'llms.txt');
  let text = readFileSync(path, 'utf8');
  if (text.includes('## Evergreen guides')) return;
  text += `\n## Evergreen guides\n\n${HUBS.map((page) => `- ${page.h1}: ${ORIGIN}/${page.slug}/`).join('\n')}\n`;
  writeFileSync(path, text);
}

writeIntentHubs();
addHubsToSitemap();
linkHubsFromBlog();
appendHubsToLlms();
console.log(`✓ SEO intent hubs: ${HUBS.length} pages generated and linked`);
