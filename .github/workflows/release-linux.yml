name: Linux release migration
on:
  workflow_dispatch:
    inputs:
      tag:
        description: Legacy input; use Signed release qualification instead
        required: false
permissions:
  contents: read
jobs:
  explain:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Standalone unsigned publishing is retired. Run Signed release qualification for arm64 and x64 builds with verified provenance.'
