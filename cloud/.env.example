# ── Stripe ──
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
STRIPE_PRICE_CLOUD=price_xxx    # $0.99/mo subscription price ID
STRIPE_PRICE_GRIZZLY=price_xxx  # $1.99/mo subscription price ID

# ── GPU Backend (where the models actually run) ──
# Point this at Ollama, vLLM, RunPod, Modal, etc.
GPU_BACKEND_URL=http://127.0.0.1:11434/v1
GPU_BACKEND_KEY=
CLOUD_MODELS=qwen2.5:7b,llama3.2:3b,deepseek-r1:7b,qwen2.5-coder:7b

# ── Server ──
PORT=4001
LANDING_URL=https://open-llm-ten.vercel.app
API_BASE_URL=https://api.llmbear.com/v1

# ── Database ──
DB_PATH=./data/llmbear.db
